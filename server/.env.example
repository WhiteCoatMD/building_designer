# Server Configuration
PORT=3001

# Email Configuration (Optional - for sending quote confirmations)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@buildingdesigner.com
ADMIN_EMAIL=admin@buildingdesigner.com

# Notes:
# - If email variables are not set, the app will still work but won't send emails
# - For Gmail, you need to create an "App Password" in your Google Account settings
# - For other email providers, adjust SMTP_HOST and SMTP_PORT accordingly
